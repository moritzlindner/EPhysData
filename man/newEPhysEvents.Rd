% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0_Class_EPhysEvents-Class.R
\name{newEPhysEvents}
\alias{newEPhysEvents}
\title{Constructor for EPhysEvents}
\usage{
newEPhysEvents(
  Data,
  TimeTrace,
  Metadata,
  Channels,
  Channel_Metadata,
  StimulusTrace = numeric(0),
  TimeUnits = "s",
  StimulusUnits = "",
  Imported = as.POSIXct(Sys.time()),
  ExamInfo = list(),
  SubjectInfo = list()
)
}
\arguments{
\item{Data}{A \emph{list} of length \code{nrow(Metadata)}; each \code{Data[[i]]} is a \emph{named list} with one element per channel. Each channel entry is a numeric vector of event timestamps.}

\item{TimeTrace}{Numeric vector of bin-center times (length \code{dim(Data)[1]}).}

\item{Metadata}{\code{data.frame} with \code{nrow(.) == dim(Data)[2]} and column \code{RunUID}.}

\item{Channels}{Character vector of channel names (length \code{dim(Data)[3]}).}

\item{Channel_Metadata}{\code{data.frame} with per-channel metadata (rows = \code{length(Channels)}).}

\item{StimulusTrace}{Numeric vector length 0 or equal to \code{TimeTrace}.}

\item{TimeUnits}{Single valid udunits symbol for time (e.g., \code{"s"}).}

\item{StimulusUnits}{Single valid udunits symbol for stimulus (may be empty).}

\item{Imported}{\code{POSIXct} import timestamp (default: current time).}

\item{ExamInfo}{List of exam-level info.}

\item{SubjectInfo}{List of subject-level info.}
}
\value{
A new \code{EPhysEvents} object.
}
\description{
Constructor for EPhysEvents
}
