% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lapply.EPhysContainer.R
\name{lapply-EPhysContainer}
\alias{lapply-EPhysContainer}
\title{Apply a function over runs × channels of EPhys containers}
\usage{
\S4method{lapply}{EPhysEvents,function}(
  X,
  FUN,
  parallel = FALSE,
  error = c("stop", "warn")[1],
  progress = interactive(),
  ...
)
}
\arguments{
\item{X}{An \code{EPhysEvents} or \code{EPhysContinuous} object.}

\item{FUN}{A function to apply to each vector (may be length 0 for events).}

\item{parallel}{logical. If \code{TRUE}, use \code{future.apply::future_lapply()} over \strong{runs}.
In parallel mode, any error stops the whole call.}

\item{error}{one of \code{c("stop","warn")}. Only used in sequential mode. In \code{"warn"}
mode, errors are warned and the corresponding result is set to \code{NULL}.}

\item{progress}{logical. Show a \code{cli} progress bar in sequential mode.}

\item{...}{Additional arguments passed to \code{FUN}.}
}
\value{
A nested list mirroring the run×channel structure:
\code{out[[run]][[channel]]}, with names taken from \code{dimnames(X@Data)$trial} (or \code{Metadata$RunUID})
and \code{dimnames(X@Data)$channel} (or \code{X@Channels}), respectively.
}
\description{
Applies \code{FUN} to each per-run×per-channel vector:
\itemize{
\item For \strong{EPhysEvents}: each element is a numeric vector of event timestamps.
\item For \strong{EPhysContinuous}: each element is the time series \code{Data[, run, channel]}.
}
}
